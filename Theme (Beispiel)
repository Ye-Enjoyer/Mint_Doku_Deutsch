#Download Theme und Extract

unzip themename.zip

#Kopiere das Theme von Donwlaods auf den grub/themes/ pfad

sudo cp -r dark-matter /boot/grub/themes/

#Grub config file ändern

sudo nano /etc/default/grub

#Bei der Zeile GRUB_THEME= auf GRUB_THEME="/boot/grub/themes/dark-matter/theme.txt" ändern.
#Achte auf den Pfad, ist nicht immer gleich

#Als letztes Grub Aktualisieren

sudo grub-mkconfig -o /boot/grub/grub.cfg
